namespace TaskManager.Models.Utilities
{
    public enum Priority
    {
        Low,
        Medium,
        High
    }

    public enum Difficulty
    {
        Easy,
        Medium,
        Hard
    }
}
